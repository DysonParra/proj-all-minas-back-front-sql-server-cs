@model IEnumerable<Project.Models.Parque>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrNombreParque)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrObservaciones)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrUbicacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrNombreParque)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrObservaciones)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrUbicacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntIdParque">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntIdParque">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntIdParque">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
